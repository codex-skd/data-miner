<h2>v1.0.1 — Project Restructure &amp; Workflow Alignment</h2>

<h3>Changed</h3>
<ul>
<li><strong>Output folder</strong>: Files now generate in <code>data_miner/info_client/</code> or <code>data_miner/info_server/</code> instead of the old flat folder names.</li>
<li><strong>Mod ID</strong>: Renamed from <code>dataminer</code> to <code>data_miner</code> across all packages, assets, configs, and JAR naming.</li>
<li><strong>Workflow</strong>: Project fully aligned to <code>WORKFLOW_GENERIC</code> v1.2.7 — branch structure, CI/CD, JAR naming, language policy.</li>
</ul>

<h3>Technical Changes</h3>
<ul>
<li><code>Initializer.java</code> — output base path updated.</li>
<li><code>.gitlab-ci.yml</code> — CI/CD restructured with fail-fast on missing <code>*/main</code> branch.</li>
<li>Git history cleaned — removed hardcoded API key from past commits.</li>
</ul>
